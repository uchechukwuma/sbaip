# Core dependencies for SBAIP — Phase 1
# Generated: 2026-08-10

# IFC Parsing
ifcopenshell==0.8.0
# Note: ifcopenshell may require pythonnet on Windows.
# If installation fails, install pythonnet first:
#   pip install pythonnet

# Data Processing
pandas==2.2.3
numpy==2.0.2

# Environment & Configuration
python-dotenv==1.0.0

# Database (Phase 2)
supabase==2.5.0
psycopg2-binary==2.9.10

# API (Phase 4 — future)
fastapi==0.115.0
uvicorn==0.31.1

# Dashboard (Phase 5 — future)
streamlit==1.38.0
plotly==5.24.0

# Data Quality (future)
great-expectations==1.0.0

# Orchestration (future — reused from Edge DataOps)
apache-airflow==2.10.0

# Utilities
python-multipart==0.0.12
pydantic==2.9.0